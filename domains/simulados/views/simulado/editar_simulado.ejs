<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!--========== BOX ICONS ==========-->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/boxicons@latest/css/boxicons.min.css">

    <!--========== CSS ==========-->
    <%- include('../partials/quill_and_uikit') %>
    <link rel="stylesheet" href="/css/global.css">
    <link rel="stylesheet" href="/css/simulado/editar_simulado.css">
 
   

    <title>Editar Simulado - IFVest</title>
</head>

<body>

    
        <main>
            <% if (typeof errorMessage !== 'undefined' && errorMessage.trim()) { %>
                <script>
                    window.onload = function() {
                        alert("<%= errorMessage %>");
                    };
                </script>
            <% } %>
            <h1>Editar Simulado</h1>

            <form action="/simulados/<%= simulado.id_simulado %>/editar?_method=PATCH" method="POST">
                <label for="titulo">Título do Simulados:</label>
                <input type="text" id="titulo" value="<%= simulado.titulo %>" name="titulo" required maxlength="100">

                <label for="descricao">Descricao:</label>
                <textarea id="descricao" name="descricao" rows="5" cols="50" required maxlength="1000"><%= simulado.descricao %></textarea>



                <label for="tipo">Selecione o tipo:</label>
                <select name="tipo" id="tipo">
                    <option value="DISSERTATIVO" <%= simulado.tipo === 'DISSERTATIVO' ? 'selected' : '' %>>Dissertativa</option>
                    <option value="OBJETIVO" <%= simulado.tipo === 'OBJETIVO' ? 'selected' : '' %>>Objetiva</option>
                    <option value="ALEATORIO" <%= simulado.tipo === 'ALEATORIO' ? 'selected' : '' %>>Aleatoria</option>
                </select><br>

                <label for="modo">Selecione o Modo:</label>
                <select name="modo" id="modo">
                    <option value="1">Normal</option>
                    <option value="2">Aleatorio</option>
                </select><br>

                <button type="submit" id="submitButton" style="width: 80%">Salvar</button>
            </form>

        </main>

        <!--========== MAIN JS ==========-->
        <script src="/js/main.js"></script>
        <script src="/js/validations/validate.js"></script>
</body>

</html>