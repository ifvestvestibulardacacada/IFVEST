<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Meus Simulados - IFVest</title>
    <%- include('../partials/libs') %>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
        <style>
            .main {
                height: 100vh;
            }

            .container {
                padding-top: 70px;
            }

            .table-responsive,
            .pagination {
                margin-bottom: 0;
            }
        </style>
</head>

<body>
    <div class="container col-lg-10 mx-auto my-4">
        <% if (typeof errorMessage !=='undefined' && errorMessage.trim()) { %>
            <script>
                window.onload = function () {
                    alert("<%= errorMessage %>");
                };
            </script>
            <% } %>
                <h1 class="border-bottom pb-2 mb-4">Meus Simulados</h1>
                <form method="GET" action="/simulados/meus-simulados" class="mb-4 d-flex align-items-end gap-2">
                    <div>
                        <label for="titulo" class="form-label">Buscar:</label>
                        <input type="text" id="titulo" name="titulo" placeholder="Filtrar por título"
                            class="form-control" maxlength="100">
                    </div>
                    <button type="submit" class="btn btn-primary"><i class="bi bi-search"></i> Pesquisar</button>
                </form>

                <div class="table-responsive">
                    <table class="table table-striped table-bordered">
                        <thead>
                            <tr>
                                <th>Título</th>
                                <th>Tipo</th>
                                <th>Ações</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% simulados.forEach(simulado=> { %>
                                <tr>
                                    <td data-bs-toggle="tooltip" data-bs-placement="top"
                                        title="<%= simulado.descricao %>">
                                        <%= simulado.titulo %>
                                    </td>
                                    <td>
                                        <%= simulado.tipo %>
                                    </td>
                                    <td>
                                        <div class="d-flex gap-2 flex-wrap">
                                            <a href="/simulados/<%= simulado.id_simulado %>/fazer"
                                                class="btn btn-outline-primary btn-sm">
                                                <i class="bi bi-play"></i> Fazer Simulado
                                            </a>
                                            <a href="/simulados/<%= simulado.id_simulado %>/editar"
                                                class="btn btn-outline-primary btn-sm">
                                                <i class="bi bi-pencil"></i> Editar
                                            </a>
                                            <a href="/simulados/<%= simulado.id_simulado %>/imprimir"
                                                class="btn btn-outline-primary btn-sm generate-pdf"
                                                data-simulado-id="<%= simulado.id_simulado %>">
                                                <i class="bi bi-file-earmark-pdf"></i> Gerar PDF
                                            </a>
                                            <a href="/simulados/<%= simulado.id_simulado %>/remover-questoes"
                                                class="btn btn-outline-primary btn-sm">
                                                <i class="bi bi-dash"></i> Remover Questões
                                            </a>
                                            <a href="/simulados/<%= simulado.id_simulado %>/adicionar-questoes"
                                                class="btn btn-outline-primary btn-sm">
                                                <i class="bi bi-plus"></i> Adicionar Questões
                                            </a>
                                            <a href="#" class="btn btn-outline-danger btn-sm delete-simulado"
                                                data-simulado-id="<%= simulado.id_simulado %>">
                                                <i class="bi bi-trash"></i> Excluir Simulado
                                            </a>
                                            <form id="deleteForm<%= simulado.id_simulado %>"
                                                action="/simulados/<%= simulado.id_simulado %>?_method=DELETE"
                                                method="POST" style="display: none;">
                                                <input type="hidden" name="_method" value="DELETE">
                                            </form>
                                        </div>
                                    </td>
                                </tr>
                                <% }); %>
                        </tbody>
                    </table>
                </div>



                <div id="modal-excluir" class="modal fade" tabindex="-1">
                    <div class="modal-dialog">
                        <div class="modal-content">
                            <form id="deleteAccountForm" action="" method="POST">
                                <div class="modal-body">
                                    <p>Você tem certeza que deseja excluir este simulado?</p>
                                </div>
                                <div class="modal-footer">
                                    <button type="submit" class="btn btn-success">Confirmar</button>
                                    <button type="button" class="btn btn-danger"
                                        data-bs-dismiss="modal">Cancelar</button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
                <!-- Modal de impressão - MOVA PARA AQUI -->
                <div class="modal fade" id="modal-imprimir" tabindex="-1">
                    <div class="modal-dialog modal-xl">
                        <div class="modal-content">
                            <div class="modal-header">
                                <h5 class="modal-title">Visualizar Simulado</h5>
                                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                            </div>
                            <div class="modal-body p-0">
                                <iframe id="iframe-imprimir" src="" frameborder="0"
                                    style="width: 100%; height: 600px;"></iframe>
                            </div>
                        </div>
                    </div>
                </div>


                                <% if (totalPages> 1) { %>
                                    <nav aria-label="Navegação">
                                        <ul class="pagination justify-content-center">
                                            <% if (currentPage> 1) { %>
                                                <li class="page-item"><a class="page-link" href="?page=1">Primeira</a>
                                                </li>
                                                <li class="page-item"><a class="page-link"
                                                        href="?page=<%= currentPage - 1 %>">Anterior</a></li>
                                                <% } %>
                                                    <% for(let i=1; i <=totalPages; i++) { %>
                                                        <li class="page-item <%= currentPage == i ? 'active' : '' %>">
                                                            <a class="page-link" href="?page=<%= i %>">
                                                                <%= i %>
                                                            </a>
                                                        </li>
                                                        <% } %>
                                                            <% if (currentPage < totalPages) { %>
                                                                <li class="page-item"><a class="page-link"
                                                                        href="?page=<%= currentPage + 1 %>">Próxima</a>
                                                                </li>
                                                                <li class="page-item"><a class="page-link"
                                                                        href="?page=<%= totalPages %>">Última</a></li>
                                                                <% } %>
                                        </ul>
                                    </nav>
                                    <% } %>
    </div>


<script>

</script>


    
    <script src="/js/main.js"></script>
    <script src="/simulados/js/simulado/meus_simulados.js"></script>
</body>

</html>