
<!DOCTYPE html>
<html lang="pt">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- Ícones -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/boxicons@latest/css/boxicons.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

  <!-- Bootstrap (para formulários e botões) -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/axios@1.6.7/dist/axios.min.js"></script>

  <!-- Estilos -->
  <link rel="stylesheet" href="/css/global.css">
  <link rel="stylesheet" href="/css/usuario/perfil_usuario.css">
  <link rel="stylesheet" href="/css/usuario/editar_usuario.css">

  <title>Perfil - <%= usuario.nome %> - IFVest</title>

  <style>

  </style>
</head>

<body>

  <!-- Mensagem de erro (se houver) -->
  <% if (typeof errorMessage !== 'undefined' && errorMessage.trim()) { %>
    <script>window.onload = () => alert("<%= errorMessage %>");</script>
  <% } %>

  <div class="container py-4">
    <div class="profile-container">

      <!-- Cabeçalho com foto e nome -->
      <div class="profile-header">
        <form action="/uploads/" method="post" enctype="multipart/form-data" id="uploadForm">
          <img 
            src="<%= usuario.imagem_perfil || '/img/profile.jpg' %>" 
            alt="Foto de perfil" 
            class="profile-image" 
            id="preview-image"
            onerror="this.src='/img/profile.jpg'">
          
          <div class="mt-3">
            <label class="upload-btn">
              Alterar Foto
              <input type="file" name="image" accept="image/*" id="image-input" style="display: none;">
            </label>
            <button type="submit" class="btn btn-light btn-sm ml-2">Salvar Foto</button>
          </div>
          <small class="d-block mt-2">JPG, PNG ou GIF • máx. 800KB</small>
        </form>

        <h3 class="mt-4 mb-1"><%- usuario.nome %></h3>
        <p class="mb-0"><i class="fa fa-user-tag"></i> <%= usuario.tipo_perfil || 'Usuário' %></p>
        <p><i class="fa fa-envelope"></i> <%= usuario.email %></p>
        
      </div>

      <!-- Corpo com formulários -->
      <div class="profile-body">

        <!-- Formulário de Edição de Dados -->
        <div class="info-card">
          <h5><i class="fa fa-user-edit text-primary"></i> Editar Informações</h5>
          <hr>
          <form id="formEditarPerfil" data-id="<%= usuario.id_usuario %>" action="/usuario/editar/<%= usuario.id_usuario %>?_method=PATCH" method="POST">
            <div class="row">
              <div class="col-md-6 mb-3">
                <label>Nome de Usuário</label>
                <input type="text" class="form-control" name="usuario" value="<%= usuario.usuario %>" maxlength="20" required>
              </div>
              <div class="col-md-6 mb-3">
                <label>Nome Completo</label>
                <input type="text" class="form-control" name="nome" value="<%= usuario.nome %>" maxlength="50" required>
              </div>
            </div>
            <div class="mb-3">
              <label>E-mail</label>
              <input type="email" class="form-control" name="email" value="<%= usuario.email %>" maxlength="100" required>
            </div>
            <div class="text-right">
              <button type="submit" class="btn btn-primary">Salvar Alterações</button>
            </div>
          </form>
        </div>

        <!-- Excluir Conta (apenas para USUARIO) -->
        <% if (perfilUsuario === "USUARIO") { %>
        <div class="info-card border-left-danger" style="border-left-color: #dc3545 !important;">
          <h5><i class="fa fa-exclamation-triangle text-danger"></i> Excluir Conta</h5>
          <hr>
          <div class="alert alert-danger small">
            <strong>Atenção!</strong> Esta ação é <strong>permanente</strong>. Todos os seus dados, simulados e progresso serão apagados.
          </div>
          <form id="formExcluirConta" data-id="<%= usuario.id_usuario %>" action="/usuario/<%= usuario.id_usuario %>?_method=DELETE" method="POST" onsubmit="return confirmDelete()">
            <div class="mb-3">
              <label>Digite <code>DELETAR</code> para confirmar:</label>
              <input type="text" class="form-control" name="confirmDelete" id="confirmInput" maxlength="10" required>
            </div>
            <div class="text-right">
              <button type="submit" class="btn btn-danger">Excluir Minha Conta</button>
            </div>
          </form>
        </div>
        <% } %>

      </div>
    </div>
  </div>

  <!-- Scripts -->
  <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
 
<script src="/js/handlers/editarUsuarioHandler.js"></script>
  <script>


    
    // Preview da imagem ao selecionar
    document.getElementById('image-input').addEventListener('change', function(e) {
      const file = e.target.files[0];
      if (file) {
        const reader = new FileReader();
        reader.onload = e => document.getElementById('preview-image').src = e.target.result;
        reader.readAsDataURL(file);
      }
    });

    // Validação de exclusão
    function confirmDelete() {
      const input = document.getElementById('confirmInput').value.trim();
      if (input !== 'DELETAR') {
        alert('Digite exatamente "DELETAR" para confirmar a exclusão.');
        return false;
      }
      return confirm('Tem certeza? Esta ação não pode ser desfeita.');
    }

    
  </script>
</body>

</html>